const baseURL = 'https://www.fixingwoo.com';

const style = {
  theme: 'light',
  neutral: 'gray',
  solid: 'contrast',
  border: 'rounded',
  surface: 'translucent',
  transition: 'all', // all | micro | macro
};

const routes = {
  '/': true,
  '/about': true,
  '/work': true,
  '/blog': true,
};

export { baseURL, style, routes };
